# BootForge Requirements
# Core dependencies
click>=8.0.0
colorama>=0.4.6
cryptography>=45.0.0
pillow>=11.0.0
psutil>=7.0.0
pyyaml>=6.0.2
requests>=2.32.0

# GUI dependencies (optional)
PyQt6>=6.9.0

# Development and packaging
pyinstaller>=6.15.0

# Distributed architecture dependencies
grpcio>=1.66.0
grpcio-tools>=1.66.0
protobuf>=5.28.0
zeroconf>=0.132.0

# Optional system tools integration
# Note: These are system packages, install separately
# Linux: badblocks, e2fsprogs, dosfstools, ntfs-3g
# macOS: diskutil (built-in)
# Windows: format (built-in)

# Optional Apple platform support
# libimobiledevice (system package for device detection, optional)
grpcio
grpcio-tools
protobuf
zeroconf
flask